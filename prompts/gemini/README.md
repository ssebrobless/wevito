# Gemini Prompt Pack

Order:

1. `icons`
2. `rat`
3. `crow`
4. `fox`
5. `snake`
6. `deer`
7. `frog`
8. `pigeon`
9. `raccoon`
10. `squirrel`
11. `goose`
12. `consistency`

Clipboard helper:

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File tools\copy-gemini-prompt.ps1 -Batch icons -OpenGemini
```

List available batches:

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File tools\copy-gemini-prompt.ps1 -List
```
